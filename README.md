# Новое русское вино

Сайт магазина авторского вина "Новое русское вино".

## О проекте

Это учебный проект на Python + Jinja2, который показывает данные о вине из Excel-файла и рендерит их в HTML.

## Требования

- Python 3.11+ или 3.14
- `Jinja2`
- `openpyxl`
- `requests`

Установите зависимости из `requirements.txt`:

```powershell
python -m pip install -r requirements.txt
```

## Запуск

1. Активируйте виртуальное окружение:

```powershell
& .venv\Scripts\Activate.ps1
```

2. Запустите сервер:

```powershell
python main.py
```

3. Откройте браузер и перейдите на:

```text
http://127.0.0.1:8000/
```

## Что делает проект

- загружает данные из Excel-файла
- группирует продукцию по категориям
- рендерит сайт через Jinja2
- показывает метки и карточки товаров

## Структура проекта

- `main.py` — HTTP-сервер и рендеринг шаблона
- `read_wine2.py` — загрузка и парсинг Excel
- `template.html` — Jinja2-шаблон страницы
- `requirements.txt` — зависимости

## Примечания

Excel-файл загружается автоматически при первом запуске, поэтому в репозитории нет нужды хранить загруженные данные.
